# E4-S7 — `codex` fixture matrix (happy + edge: BOM, CRLF, comments, symlink, missing, mixed indent, unknown providers)

**User story.**
As a claudecm developer, I want a pinned `testdata/codex/{happy,edge}/` corpus that exercises every NFR-T1 case so the Codex adapter cannot regress silently.

**Acceptance criteria.**
- `testdata/codex/happy/canonical_minimal/{config.toml,auth.json}` + `canonical_maximal/...` exist.
- `testdata/codex/edge/` covers: `missing_config/`, `missing_auth/`, `partial/`, `bom/`, `crlf/`, `mixed_indent/`, `comments/`, `symlink/`, `unknown_providers/`, `separate_fs/`.
- Each fixture has golden expected outputs for `Import`, `Plan`, `Apply`, `Project`.
- Round-trip `import → switch → export → explain` byte-identical on owned-key scope for every fixture.
- Coverage on `internal/adapter/codex` ≥ 80% after this story.

**PRD/architecture refs.** PRD NFR-T1, SM-2, SM-4; Architecture §11; coding-standards "Testing Mandates".

**Test plan.**
- Happy: minimal + maximal round-trip golden equality.
- Edge: every directory above asserted; symlink fixture refused on out-of-`$HOME` resolve; comments fixture preserved via TOML doc model.

**No fallback writes.** Malformed edge cases refused. NFR-S1.

**Complexity.** M.

**Dependencies.** E4-S5.

**Ready-for-dev checklist.**
- [x] PRD/architecture refs correct.
- [x] AC testable.
- [x] Happy + edge test rows.
- [x] No-fallback reminder present.
- [x] V1-scope only.
- [x] Deps satisfied.
- [x] Complexity set.
