# E4-S6 — `codex`: `Project` → `EffectiveView`

**User story.**
As a Codex CLI user, I want `claudecm current` and `claudecm explain` to show what Codex will actually see, so I can verify provider, base_url, key source, and model before kicking off a session.

**Acceptance criteria.**
- Given an active profile + on-disk `config.toml` + `auth.json`, when `codex.Project(profile)` runs, then it returns an `EffectiveView` with `WinningLayer`, `Source`, and `ShadowedLayers` per field.
- Default redaction (NFR-S8): `OPENAI_API_KEY` rendered as `sk-***last4` unless `--reveal`.
- External drift: SHA256 cross-check vs `State.LastAppliedPerTool[codex]` for both files; mismatch → `ExternalDriftDetected = true` with file path.
- `Project` does not write; it is read-only.

**PRD/architecture refs.** PRD FR-6, FR-7, NFR-S8, NFR-E1; Architecture §6, §6.2.

**Test plan.**
- Happy: profile sets `base_url` + `model_provider` → reported, redaction applied.
- Edge: env var `OPENAI_API_KEY` set → EnvOverride wins; `~/.codex/config.toml` mutated externally → drift detected.

**No fallback writes.** N/A — read-only.

**Complexity.** M.

**Dependencies.** E4-S4, E5-S1.

**Ready-for-dev checklist.**
- [x] PRD/architecture refs correct.
- [x] AC testable.
- [x] Happy + edge test rows.
- [x] No-fallback reminder present.
- [x] V1-scope only.
- [x] Deps satisfied.
- [x] Complexity set.
