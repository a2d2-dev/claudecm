# E9-S1 — README rewrite (cc-switch voice; concrete, narrow, useful today)

**User story.**
As a new user landing on claudecm's GitHub page, I want a README that, in 60 seconds, tells me exactly what this tool does, who it's for, and how to do my first `switch`.

**Acceptance criteria.**
- README is rewritten to the locked v1 scope: Claude Code + Codex CLI only; primary activation = direct write through FR-5; secondary = `export`; storage = plaintext YAML at `0600`.
- No AES, no MCP, no GUI, no Gemini CLI, no Cursor/Windsurf, no IDE plugins, no "secure storage" claims.
- Includes a 4-step quickstart: install → `import claude-code` (or `import codex`) → `add` (or skip) → `switch`.
- Cites SM-5 (≤ 3 min) target inline.
- Links to `docs/prd/prd-v1.md` and `docs/architecture.md` as the deeper-reading paths.
- Runs in parallel with E5/E6; does not block the implementation queue.

**PRD/architecture refs.** PRD §1, §2, §4.6, SM-5, NFR-D1; Architecture §0.

**Test plan.**
- Happy: README renders cleanly on GitHub; quickstart commands work on a clean machine.
- Edge: CI grep guard (E8-S6) catches any reintroduced cryptographic claim.

**No fallback writes.** N/A — docs.

**Complexity.** M.

**Dependencies.** None (can start when E5-S5 ships).

**Ready-for-dev checklist.**
- [x] PRD/architecture refs correct.
- [x] AC testable.
- [x] Happy + edge test rows.
- [x] No-fallback reminder present.
- [x] V1-scope only.
- [x] Deps satisfied.
- [x] Complexity set.
