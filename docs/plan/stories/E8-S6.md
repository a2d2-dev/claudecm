# E8-S6 — AES-claim scrub release gate (NFR-D1) — closed via PR #3

**User story.**
As a claudecm maintainer, I want CI to grep for forbidden cryptographic-claim strings in docs and code so the NFR-D1 release gate cannot regress.

**Status.** **Closed by PR #3** (AES-claim scrub of `README.md`, `brief.md`, `project-brief.md`, `architecture.md`). This story tracks the **CI guard** that prevents regression.

**Acceptance criteria.**
- CI step runs `grep -REni 'AES-?256|secure storage|encryption at rest' README.md docs/ cmd/ internal/ pkg/` and fails on any hit outside an allowlist (e.g., this story file's literal example, and ADR-0001 §Locked Decisions which uses "encryption" to say it is **deferred**).
- The allowlist is a small file of regex exceptions, reviewed in PR.
- Adding `crypto/aes` (or any encryption library) anywhere outside an explicit post-v1 ADR is blocked at lint level.
- Release-gate checklist references this story as closed.

**PRD/architecture refs.** PRD NFR-D1, §5 ("Non-Goals"); ADR-0001 Decision 6; coding-standards rule 14.

**Test plan.**
- Happy: clean tree → green.
- Edge: add a README line saying "AES-256 secure storage" → CI red; add `import "crypto/aes"` → CI red.

**No fallback writes.** N/A — CI guard only.

**Complexity.** S.

**Dependencies.** None (PR #3 already merged).

**Ready-for-dev checklist.**
- [x] PRD/architecture refs correct.
- [x] AC testable.
- [x] Happy + edge test rows.
- [x] No-fallback reminder present.
- [x] V1-scope only.
- [x] Deps satisfied.
- [x] Complexity set.
