# E3-S7 — `claudecode` fixture matrix (happy + edge: BOM, CRLF, comments, symlink, missing, unknown-keys-mixed)

**User story.**
As a claudecm developer, I want a pinned `testdata/claudecode/{happy,edge}/` corpus that exercises every NFR-T1 case so the adapter cannot regress silently.

**Acceptance criteria.**
- `testdata/claudecode/happy/canonical_minimal.json`, `canonical_maximal.json` exist.
- `testdata/claudecode/edge/` covers: `missing/`, `partial/`, `unknown_keys/`, `bom/`, `crlf/`, `mixed_indent/`, `comments/`, `symlink/`, `separate_fs/`.
- Each fixture has a golden expected output for `Import`, `Plan`, `Apply`, `Project`.
- Round-trip `import → switch → export → explain` byte-identical on owned-key scope for every fixture (SM-2).
- Coverage on `internal/adapter/claudecode` ≥ 80% after this story.

**PRD/architecture refs.** PRD NFR-T1, SM-2, SM-4; Architecture §11; coding-standards "Testing Mandates".

**Test plan.**
- Happy: minimal + maximal round-trip golden equality.
- Edge: every directory above asserted; symlink fixture refused on out-of-`$HOME` resolve.

**No fallback writes.** Edge cases assert refuse-on-malformed, not silent rewrite. NFR-S1.

**Complexity.** M.

**Dependencies.** E3-S5.

**Ready-for-dev checklist.**
- [x] PRD/architecture refs correct.
- [x] AC testable.
- [x] Happy + edge test rows.
- [x] No-fallback reminder present.
- [x] V1-scope only.
- [x] Deps satisfied.
- [x] Complexity set.
